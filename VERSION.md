# 版本记录

> 本文件是 CouchArcade 的**版本与阶段规划**唯一来源。
> 代码侧的版本号常量在 `assets/scripts/config/AppConfig.ts` 的 `APP_VERSION`，发版时两边同步。

---

## 当前版本

| 项 | 值 |
| :--- | :--- |
| **版本号** | `0.1.0` |
| **代号** | 初始版本（Initial） |
| **阶段** | 阶段一 · Cocos 编辑器内页面调试 |
| **日期** | 2026-09-15 |
| **状态** | 🚧 进行中 |

### 这个版本是什么

第一阶段的可运行初版：**4 个场景（Loading → Lobby → Room → Game）、2 款游戏（寻机头 + 五子棋）、7 个平台能力接口全部预留、Mock 实现可在编辑器内全流程游玩。**

当前**只在 Cocos Creator 3.8.8 编辑器内进行页面调试** —— 所有平台能力走 `MockXxx` 实现（`AppConfig.USE_MOCK = true`），微信侧接口以桩形式预留（`WxXxx` + `TODO(wechat-phase2)`），不做任何真实微信调用、不构建、不部署。

### 这个版本已验证

| 检查项 | 命令 | 结果 |
| :--- | :--- | :--- |
| TypeScript 严格模式 | `.\typecheck.cmd` | ✅ `TYPECHECK_EXIT=0` |
| 核心算法自测（40 项） | `node tools/test-core.js` | ✅ 40 通过 / 0 失败 |
| 场景结构与内容校验 | `node tools/validate-scenes.js` | ✅ `ALL_SCENE_VALIDATIONS_PASSED` |
| 云函数语法 | `node --check` | ✅ 9 函数 + 公共模块全部通过 |

### 这个版本未验证

- ⚠️ **编辑器预览的实际可玩性**（UI 布局、触摸响应、动画表现）—— 需人工肉眼验收，见 `docs/SELF_TEST_CHECKLIST.md`
- ⚠️ 真机表现、微信接口联通、云函数实际部署 —— 属于阶段二范围

### 已知遗留

见 `README.md` 第 8 节「已知事项与后续建议」共 7 项。其中需在阶段二处理的关键项：

- 寻机头布局的读权限风险（`games_planehunt` 的 `cells` 字段需拆表）
- 头像为文字占位、音效未接入
- 加入房间为简化交互（随机房间号直进）

---

## 阶段规划

整个项目分三个阶段推进，**先打通全流程，再不断扩充游戏**。

```
阶段一              阶段二                    阶段三            之后
编辑器内调试   →   微信开发者工具挂接    →   上线        →   持续扩充游戏
Cocos / Mock       SDK + 广告主             提审发布        不断加新游戏
   ▲
   └─ 当前在这里（v0.1.0）
```

### 阶段一 · Cocos 编辑器内页面调试 ✅ 进行中

- **目标**：编辑器内全流程可玩，把玩法、UI、协议链路调对
- **手段**：`MockXxx` 实现 + `AppConfig.USE_MOCK = true`，零 `wx.*` 依赖
- **不做**：不构建、不部署、不调真实微信接口
- **出口条件**：`docs/SELF_TEST_CHECKLIST.md` M1~M5 全部通过
- **对应版本**：`0.1.x`

### 阶段二 · 微信开发者工具：SDK 与广告挂接

- **目标**：把预留的桩变成真实现，跑通真机全链路
- **手段**：实现 `WxXxx`（登录 / 房间 / 同步 / 分享 / 存储 / 云函数），接入 `wx.cloud`，部署 9 个云函数
- **SDK 挂接**：微信登录、用户信息、分享转发、订阅消息
- **广告主挂接**：激励视频 / 插屏广告位接入与回调（作为变现方式，需在真机验证）
- **切换方式**：`AppConfig.USE_MOCK = false`（业务层零改动）
- **操作手册**：`docs/WECHAT_INTEGRATION_CHECKLIST.md`（15 项联通清单）
- **对应版本**：`0.2.x`

### 阶段三 · 上线

- **目标**：提审通过并发布
- **内容**：正式 appid、提审材料、隐私协议、体验优化、异常兜底、性能与包体确认
- **对应版本**：`1.0.0`

### 阶段四之后 · 持续扩充游戏

全流程打通后，进入**不断加游戏**的长期阶段 —— 这正是当前架构的设计意图：

> 新增一款游戏只需 3 步：登记 `GameList` → 实现 `IGame` → 注册 `GameRegistry`。
> 大厅、房间、对局场景**无需任何改动**（全部配置驱动）。

详见 `README.md` 第 4.4 节。

---

## 版本号规则

采用 `主版本.次版本.修订号`（SemVer 简化版）：

| 位 | 何时递增 | 例 |
| :--- | :--- | :--- |
| **主版本** | 阶段发生质变（上线、架构重写） | `0.x.x` → `1.0.0`（上线） |
| **次版本** | 新增游戏 / 新增平台能力 / 不兼容变更 | `0.1.0` → `0.2.0`（接微信） |
| **修订号** | Bug 修复、UI 调整、参数改动 | `0.1.0` → `0.1.1` |

**与微信平台版本号的关系**：微信小游戏自身的版本号由平台管理，与本文件的版本号**不必一致**，但建议发版时同步，避免排查时对不上号。

---

## 版本历史

| 版本 | 日期 | 阶段 | 摘要 |
| :--- | :--- | :--- | :--- |
| **0.1.0** | 2026-09-15 | 一 | **初始版本**。4 场景 + 2 游戏 + 平台抽象层 + Mock 架构；编辑器内可全流程游玩；微信接口全部预留。<br>本版本期间修复：场景序列化格式（4 类）、服务未注入、UI 改为静态节点、浮层容器缺 `UITransform` 导致不可见、微信明确加载页 —— 全过程见 `FIX_REPORT.md` |
