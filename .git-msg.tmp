chore(release): v0.1.1 里程碑 —— Cocos 侧所有功能基本可用

4 场景（Loading→Lobby→Room→Game）全链路贯通，2 款游戏（寻机头/五子棋）
均可与 AI 完整对局到结算，UI 可见、可点、可操作。Cocos 侧功能开发告一段落。

修复的拦路 bug（详见 FIX_REPORT.md 第 6~8 轮）：
- 运行时节点落在 DEFAULT 层 → 相机看不见、无命中测试
  （「点开始游戏没反应」的真因；统一走 UIFactory.newUINode）
- PlaneHuntAuthority 实参错位 → 寻机头回合交接断裂
  （「点棋盘没反应」的真因：secondPlayerId 被传成 myPlayerId）
- 客户端回合同步缺失（_serverTurnId 以权威下发为准）
- 练习房被 _refreshReadyStatus 降级为 WAITING
  （「AI 练习报全员准备后才能开局」的真因）
- 寻机头得分归属错位（按翻格者 byPlayerId 归属，不再用回合反推）
- 回合指示 _myTurnLabel/_oppTurnLabel 指向同一节点、互相覆盖
- 弹窗选项绝对定位会压到取消按钮 → 改 ScrollView + 分区高度

体验与规则：
- 寻机头翻到机头不再奖励连翻，改为直接换手
- 模式弹窗选项收紧（88→72）、取消按钮独立成区
- 棋盘加粗外框 + 两盘线条同色同粗（抽到 BoardBase 共用）
- 棋盘配色：盘底暖灰白、网格线加深、白子加深描边
- Game HUD 重排：左右双栏（对手|我）+ ◆ 回合高亮 + 昵称提色
- 寻机头 HUD 新增「已找到机头 n / 5」

质量护栏（都可证伪）：
- validate-scenes 新增「运行时节点层级护栏」：new Node() 未设 UI_2D 层即失败
- validate-scenes 新增「Game HUD 契约 + 纵向不重叠」：改名/挪位/压版即失败
- test-core 40 → 52 项，新增寻机头回合交接与归属契约的回归断言

验证：typecheck EXIT=0；validate-scenes ALL_SCENE_VALIDATIONS_PASSED；
test-core 52 通过 0 失败；云函数语法全部通过。
版本号双处同步（AppConfig.APP_VERSION 与 VERSION.md）。
